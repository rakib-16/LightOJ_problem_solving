for _ in range(int(input())):
    sentence = input().split(" ")
    length = len(sentence)
    print(f"Count = {length}")
    # for word in sentence:
    #     if word != " ":
            # sentence.pop(word)
            # length = len(sentence)
            # print(f"Count = {length}")
        # else:
        #     print(f"Count = {length}")